import 'package:dalali_hub/data/remote/model/realm/realm_models.dart';

class RoomWrapper {
  final String currentUserId;
  final Rooms room;

  RoomWrapper({required this.currentUserId, required this.room});
}
